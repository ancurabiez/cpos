********* Parsing example **********

struct isofield_cfg *ifield;
struct isomsg *imsg;
int i;

ifield = cpos_init("cpos.cfg");
if (!ifield)
  return;
  
imsg = cpos_parse_init();
if (!imsg)
  return;
  
if (cpos_parse(ifield, imsg, ISO8583_MESSAGES))
  return;
  
for (i = 0; i < ISO_BIT_LEN; i++) {
  if (imsg[i].data)
    printf("BIT [%d] [%s]\n", i, imsg[i].data);
}

cpos_parse_free(imsg);
cpos_close(bc);


********** Build example *************

struct isofiled_cfg *ifield;
struct isomsg *imsg;
char buffer[128];

ifield = cpos_init("cpos.cfg");
if (!ifield)
  return;
  
imsg = cpos_build_init();
if (!imsg)
  return;

cpos_build_set_field(bm, 7, "0118105707", 10);
cpos_build_set_field(bm, 11, "00000100", 8);
cpos_build_set_field(bm, 39, "00", 2);
cpos_build_set_field(bm, 70, "001", 3);

/* build without length header */
cpos_build_msg(ifield, imsg, "0810", buffer, sizeof(buffer), 0);
printf("Build without len: [%s]\n", buffer);

/* build with length header */
cpos_build_msg(ifield, imsg, "0810", buffer, sizeof(buffer), 1);
printf("Build with len: [%s]\n", buffer);

cpos_build_free(imsg);
cpos_close(ifield);
